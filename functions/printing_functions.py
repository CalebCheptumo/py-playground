def print_model(model):
    """Print a 3D model"""
    print(f"Printing model: {model}")

def print_models(models):
    """Print a list of 3D models"""
    for model in models:
        print_model(model)